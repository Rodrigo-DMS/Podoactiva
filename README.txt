Install all required libraries in their latest version, requirements.txt contains certain libraries that may require a specific version, but not all.
run runmodel.py to run the model on a video. Must provide video path and directory where yolact folder is located.
Video should be recorded in a similar way to the sample video provided and of a similar length.
Important: for better performance run on GPU using CUDA 12.1
